"""

适配器模式，代理模式，装饰器模式写法接近
1. 适配器模式（Adapter Pattern）

目的：将一个类的接口转换成客户端期望的另一个接口，使原本接口不兼容的类可以一起工作。
特点：
通过包装原有类，使其接口符合目标接口。
适用于需要使用现有类，但其接口与需求不匹配的情况。

7. 代理模式（Proxy Pattern）

目的：通过引入代理对象来控制对原始对象的访问，通常用于延迟实例化、访问控制等场景。
特点：
代理对象与原始对象实现相同的接口，控制对原始对象的访问。
适用于需要控制对象访问或添加额外处理的场景。

4. 装饰器模式（Decorator Pattern）

目的：动态地给一个对象添加额外的职责或功能。
特点：
通过创建装饰器类，包装原始对象，添加新的功能。
适用于需要在运行时增加功能的场景，遵循开闭原则。

2. 桥接模式（Bridge Pattern）

目的：将抽象部分与实现部分分离，使它们可以独立变化。
特点：
通过组合而非继承，将抽象和实现解耦。
适用于需要在多个维度进行扩展的系统。
3. 组合模式（Composite Pattern）

目的：将对象组合成树形结构，以表示“整体-部分”的层次关系。
特点：
使客户端对单个对象和组合对象的处理具有一致性。
适用于需要表示部分-整体层次结构的场景。

5. 外观模式（Facade Pattern）

目的：为复杂的子系统提供一个统一的接口，简化客户端与子系统之间的交互。
特点：
通过提供高层接口，隐藏系统的复杂性。
适用于需要简化接口或减少系统耦合度的场景。
6. 享元模式（Flyweight Pattern）

目的：通过共享对象来减少内存使用，以支持大量细粒度的对象。
特点：
将对象的状态分为内部状态和外部状态，内部状态可以共享。
适用于需要大量对象且对象之间有共享部分的场景。

"""
