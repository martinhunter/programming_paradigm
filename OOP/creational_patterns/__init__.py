"""
1. 单例模式（Singleton Pattern）
目的：确保某个类只有一个实例，并提供全局访问点。
特点：
保证类只有一个实例，避免多次创建对象。
通常通过私有构造方法和静态方法来提供对唯一实例的访问。
适用于需要全局访问并且只能有一个实例的场景，如数据库连接池或日志管理器。
2. 工厂方法模式（Factory Method Pattern）
目的：定义一个创建对象的接口，但由子类决定实例化哪个类。
特点：
将对象的实例化推迟到子类，由子类来决定创建哪种类型的对象。
通过工厂方法封装了对象的创建过程，避免了在代码中硬编码具体的类名。
适用于希望将对象的创建和使用分离的场景，例如在不同操作系统中创建不同类型的文件系统。
3. 抽象工厂模式（Abstract Factory Pattern）
目的：提供一个创建一系列相关或相互依赖对象的接口，而无需指定具体类。
特点：
创建一组相关的对象，而不仅仅是单个对象。
每个具体工厂类生成一系列相关的产品，确保产品之间的一致性。
适用于产品族的创建，且需要确保产品之间的兼容性，例如在不同操作系统下生成一系列UI控件。
4. 建造者模式（Builder Pattern）
目的：将一个复杂对象的构建与它的表示分离，使得同样的构建过程可以创建不同的表示。
特点：
用于构建复杂对象的构建过程与最终表示分离，使得不同类型的产品能使用相同的构建过程。
通过构建器类逐步构建产品对象，可以在构建过程中灵活定制产品的各个部分。
适用于需要逐步构建、细节多且不易组合的对象，例如HTML页面或复杂文档的生成。
5. 原型模式（Prototype Pattern）
目的：通过复制现有对象来创建新的对象，而不是通过新实例化。
特点：
通过克隆已有的对象来创建新对象，避免了重复创建复杂对象的过程。
适用于创建大量相似对象时，可以减少内存消耗和时间消耗。
通常通过实现Cloneable接口或自定义克隆方法来实现对象的复制。
"""
